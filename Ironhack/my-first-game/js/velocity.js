
/*

let imageArray = ["images/eggpixel.jpg", "images/pixelcake.jpg", "images/pixeldog.jpg",
"images/pixelduck.jpg" ];

function dropImage(){
    let height = (Math.random() * 35) + 50; //length let height = Math.floor(Math.random()*100);
    let velocity = Math.random() * (3 - 1) + 1;  //velocity
    let width = (Math.random() * 35) + 50; //size let width = Math.floor(Math.random()*100);
  let randomElement = imageArray[Math.floor(Math.random() * imageArray.length)]
  console.log(randomElement)
  
  
    game.imageArray.push(new Obstacle(game, height, velocity, width, randomElement))
  }

  dropping() {
        this.y += this.velocity;
    }
*/




